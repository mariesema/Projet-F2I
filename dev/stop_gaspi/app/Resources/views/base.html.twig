<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}StopGaspi{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" href="{{ asset('assets/vendor/bower_components/bootstrap/dist/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('assets/css/stopgaspi_custom.css') }}">
        <link rel="stylesheet" href="{{ asset('assets/vendor/bower_components/bootstrap/dist/css/business-casual.css') }}">
    </head>
    <body>
        <div class="brand"><a class="stop_green" href="{{ path('homepage') }}">Stop Gaspi</a></div>

        <div class="address-bar">A JMM Website | Vincennes 94300 | 06.65.79.92.77</div>
        <!-- Navigation -->
        <nav class="navbar navbar-inverse" role="navigation">
            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li>
                            <a href="{{ path('fiche_index') }}">Fiches Conseil</a>
                        </li>
                        <li>
                            <a href="{{ path('conseiller_index') }}">Contact</a>
                        </li>
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                            <li>
                                <a href="#">My account</a>
                            </li>
                            <li>
                                <a href="{{ path('logout') }}">Déconnexion</a>
                            </li>
                        {% else %}
                        <li>
                            <a href="{{ path('user_registration') }}">Enregistrement</a>
                        </li>
                        <li>
                            <a href="{{ path('login') }}">Connexion</a>
                        </li>
                        {% endif %}
                    </ul>
                </div>
                <!-- /.navbar-collapse -->
            </div>
            <!-- /.container -->
        </nav>

        <!-- Image Background Page Header -->
        <!-- Note: The background image is set within the business-casual.css file. -->
        <div class="container">
        {% block body %}{% endblock %}
        </div>
        {% block javascripts %}
            <script type="text/javascript" src="{{ asset('assets/vendor/bower_components/jquery/dist/jquery.min.js') }}"> </script>
            <script type="text/javascript" src="{{ asset('assets/vendor/bower_components/bootstrap/js/modal.js') }}"> </script>
        {% endblock %}
    </body>
</html>


