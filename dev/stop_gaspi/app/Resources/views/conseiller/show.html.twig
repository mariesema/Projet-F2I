{% extends 'base.html.twig' %}

{% block body %}
    <h1>Conseiller</h1>

    <table>
        <tbody>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('conseiller_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('conseiller_edit', { 'id': conseiller.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
